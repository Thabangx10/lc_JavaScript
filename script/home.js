// Define the variables and concatenate them: In the script.js file, define the variables and concatenate them using the + operator.

let firstName = 'Thabang';
let lastName = 'Kganana';
let fullName = firstName + " " + lastName;
console.log(fullName);

// Updating the content in our <p></p>, using its id.

document.getElementById('Th').innerHTML = fullName;
// console.log();


// Another variable

numberVar = 5
console.log(numberVar * 5);

// "Const" var

// const name = 'thabang'
// const name = 'Kganana'

// -> We get an error due to us redaclring the same variable more than once (a blocked scoped variable)